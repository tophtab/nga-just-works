# Planning review — 2026-09-11 revised scope

> Historical review below records the planning gate on 2026-09-11. The final
> section records the user's 2026-09-12 approval; the task is now in_progress
> in the isolated compatibility worktree. The old next-step instruction is
> completed, not a request to seek the same approval again.

## Result

Revised planning is ready for the user to review. Existing task creation/reuse authorization persists; the old restrictive summary is superseded. Current status remains planning. The latest concrete summary must be presented and approved before task activation or product implementation.

The user wants maximum practical reuse of completed useful upstream capabilities, plus the integration needed by this fork. Code that only declares an unused sidecar field is not a completed feature; no undocumented standalone attachment, hot-reply, parent-comment, or score protocol is added.

## Concrete final summary

- Goal: keep upstream-compatible full-thread, PID/single-reply and author-filter reading in the current native reader.
- In scope: default-off laboratory setting; upstream App request/DTO/row conversion and quote syntax; source-consistent paging/anchors; UID OP and explicit comment menu behavior; identity/lifecycle/terminal handling; old cache compatibility plus explicit owner/source/layout storage and cache-entry guard.
- Preserve: current full-prefix image handling, manual host rules, ordinary next-two prefetch/final-page freshness, latest menu/cache/metadata/tabs fixes, user source text, anonymity/client details and old normal parsing behavior.
- Acceptance: synthetic full/PID/author/variable-page cases; optional metadata remains readable; real targets are found without modulo jumps; source/query/account changes cannot reuse stale data; cache layouts do not collide; meaningful JVM/fake transport checks, debug build and every module lint XML at zero Error/Fatal.
- Deferred: independent board-icon/video batches remain in the complete adoption ledger; whole model/JSON/SDK migrations are separate batches. No new standalone decoding protocol for fields upstream never implemented. New-format cache zip is deferred; legacy import/export remains explicit.
- Limits: no genuine App response fixture/live availability verification; source normalization and missing metadata behavior are deliberate local policies. Unknown core content is visibly incomplete and not cached. Source changes may need one bounded alignment request, with no page-scanning retry loop.
- Artifact status: PRD/design/implement rewritten, source/revision research saved, previous design/implementation/review archived under history, both curated manifests validated.

## Verification performed

- task.py validate passed: 18 real unique entries in each manifest.
- Three injected files exceed the 32,768-byte per-file cap: android-quality-guidelines.md (36,146), component-guidelines.md (39,634), query-pagination-revision.md (39,995). Manifests and implement.md require implement/check agents to read the original files with tools; truncated injection does not count as complete context.
- Current PRD/design/implement/scope/adoption files passed local-link, fence and whitespace checks; R1–R8 and AC1–AC8 are present. JSON/JSONL parse and referenced-file checks passed.
- PRD convergence reread completed: goal, source facts, selected requirements, acceptance and deferred work are explicit. Technical choices are in design/implement; no remaining repository-answerable question is being passed to the user.
- git diff --check passed. No tracked product diff was created. These checks verify planning artifacts, not product behavior; no build or product test ran in planning.

## Workspace

HEAD stayed 5bb92cf033aa32d749d10e1a497bc05173cd2955 and includes 6203dad5. The workspace branch changed externally from fix/thread-menu-cache to main at the same commit; the main task did not switch branches, merge or commit. The recorded implementation baseline is now main at that exact SHA.

Unrelated loading/IP-location task directories remain untouched. Recheck workspace changes before implementation and preserve other work. No task activation, NGA/account/device operation, push, release or product implementation was performed.

## Required next step

Present the complete August explanation and revised concrete scope, then obtain one approval for implementing this compatibility batch. The phase gate is required by .agents/skills/trellis-brainstorm/SKILL.md:60–61. Existing task consent does not need to be repeated; none of the archived restrictions should be used as the current plan.

## Implementation approval — 2026-09-12

The revised final scope was presented, then terminology was clarified without changing the scope. The user replied 「行」. This approves implementation of the compatibility batch. Do not ask for task creation or the same implementation approval again.
